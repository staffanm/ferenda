.git
doc
tng.lagen.nu

# Trim the ferenda build context (docker/ferenda/Dockerfile, whose
# context is the repo root). The generated corpus is bind-mounted at runtime,
# not baked; the venv and dev config must not leak into the image.
.venv
site
config.yml
.env
.env~
mediawiki-db
__pycache__
.pytest_cache
.ruff_cache
