"""Hand-authored value-add from the lagen.nu MediaWiki dump: SFS commentary
(`kommentar`) anchored to statute paragraphs, and the concept/keyword glossary
(`begrepp`). Both are ordinary sources — wikitext parsed (with the shared
citation engine) into artifacts that flow through the same catalog → inbound
graph → render pipeline as the machine-extracted sources.
"""
