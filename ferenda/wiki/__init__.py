"""Hand-authored value-add from the ``lagen-wiki`` git-backed markdown repo
(migrated off the old lagen.nu MediaWiki): SFS/EU commentary (`kommentar`)
anchored to statute paragraphs and articles, and the concept/keyword glossary
(`begrepp`). Both are ordinary sources — markdown parsed (with the shared
citation engine) into artifacts that flow through the same catalog → inbound
graph → render pipeline as the machine-extracted sources.
"""
