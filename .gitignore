*~
devtools.py
*.pyc
*.log
@eaDir
.DS_Store
/defaultDB/
/ferendaDB/
/lagenDB/
.ropeproject
/.coverage
/.tox
/dist/
/ferenda.egg-info/
/htmlcov/
/netstandards
/lagen.nu
/test/files/repo/
/tng.lagen.nu
/wip
__pycache__/
/doc/_build/
/data/
/.direnv/
# Version-control .claude/ (hooks, settings, agents, skills) except the
# genuinely machine-local files: personal permission overrides and the
# scheduler's runtime lock.
/.claude/settings.local.json
/.claude/scheduled_tasks.lock
/.env
/mediawiki-db-backup-230502/
/mediawiki-html-backup-230502/
# Local MediaWiki SQLite source for the one-time markdown migration (not tracked;
# tools/mediawiki_to_markdown.py reads it to replay history into ../lagen-wiki)
/mediawiki-db/
# Stray local package downloads
*.deb
/site/data/
.playwright-mcp/
opencode.json
# Site config holds live credentials -- never track
/site/ferenda.ini
# Machine-local runtime config (data_root etc.)
/config.yml

# Vendored POI jars (fetch via tools/fetch_poi.sh)
vendor/poi/*.jar
